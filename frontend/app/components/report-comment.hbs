<textarea
  type="text"
  class="form-textarea comment-field rounded
    {{if @customerVisible 'customer-comment'}}
    {{if @value 'line-clamp'}}
    {{this.heightClass}}"
  placeholder="Comment"
  name="comment"
  id="row-comment"
  value={{@value}}
  disabled={{@disabled}}
  title={{this.tooltip}}
  spellcheck="true"
  autocomplete="true"
  wrap="soft"
  rows="1"
  {{on "input" (pick "target.value" @onChange)}}
  {{on "keydown" this.handleKeydown}}
  data-test-report-comment
/>