<div class="sy-checkbox checkbox" ...attributes>
  <input
    type="checkbox"
    class="rounded checked:text-adfinis-green"
    id={{this.checkboxElementId}}
    checked={{@checked}}
    disabled={{@disabled}}
    indeterminate={{eq @checked null}}
    {{on "change" (pick "target.checked" (optional @onChange))}}
  />
  <label class="text-black" for={{this.checkboxElementId}} title={{@title}}>
    {{#if (has-block)}}
      {{yield}}
    {{else}}
      {{#if @label}}
        {{@label}}
      {{else}}
        &nbsp;
      {{/if}}
    {{/if}}
  </label>
</div>
