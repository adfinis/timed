<div
  class="{{if @option.archived 'inactive'}} flex justify-between items-center"
  title="{{if @option.isTask @option.longName @option.name}}{{if
    @option.archived
    ' (archived)'
  }}"
>
  {{#if @option.isTask}}
    <span class="history history-text inline-flex flex-col xl:text-lg">
      <small
        class="text-2xs xl:text-xs text-[color-mix(in_srgb,currentColor,transparent_10%)] mt-0.5 -mb-0.5"
      >{{@option.project.customer.name}}
        &gt;
        {{@option.project.name}}</small>
      {{@option.name}}
    </span>
  {{else}}
    {{@option.name}}
  {{/if}}
  {{#if @option.archived}}
    <FaIcon @icon="archive" @prefix="fas" />
  {{/if}}
</div>